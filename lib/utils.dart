const String HOST = 'localsocialnetwork.herokuapp.com';

enum StropheStatus {
    ERROR,
    CONNECTING,
    CONNFAIL,
    AUTHENTICATING,
    AUTHFAIL,
    CONNECTED,
    DISCONNECTED,
    DISCONNECTING,
    ATTACHED,
    REDIRECT,
    CONNTIMEOUT,
    REGIFAIL,
    REGISTER,
    REGISTERED,
    CONFLICT,
    NOTACCEPTABLE
}
